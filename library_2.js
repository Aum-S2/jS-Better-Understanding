// var libraryName = "lib 2";
window.libraryName = window.libraryName || "lib 2"; 
//this is to check the global namespace, or the global name object to see if something of that name is there, so it doesnt collide or override it.